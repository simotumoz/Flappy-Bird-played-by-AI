INPUTS -> bird.y, top_pipe, bottom_pipe
OUTPUTS -> jump
ACTIVATION FUNCTION -> tanh(x), values in (-1,1), if val > 0.5 proceed
POPULATION SIZE -> 100
FITNESS FUNCTION -> distance
MAX GENERATIONS -> 30

